import { StatusBar } from 'expo-status-bar';
import { StyleSheet, Text, View, Image, Button } from 'react-native';
import StartStack from './navigate'


export default function App() {
  return (
    <StartStack/>
  );
}

const styles = StyleSheet.create({
  
});
